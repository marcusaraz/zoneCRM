name: Zone CRM check

on:
  push:
    branches: [zone]
    paths:
      - 'packages/twenty-server/**'
      - 'packages/twenty-front/**'
      - 'packages/twenty-shared/**'
      - 'packages/twenty-ui/**'
      - '.github/workflows/zone-check.yaml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  server:
    timeout-minutes: 40
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        uses: ./.github/actions/yarn-install
      - name: Build twenty-shared
        run: npx nx build twenty-shared
      - name: Server typecheck
        run: npx nx run twenty-server:typecheck
      - name: Server lint
        run: npx nx run twenty-server:lint
      # Decision 12 asked for a test of each refusal. A test that is only
      # typechecked is not a test, and typecheck and lint were all this job ran,
      # so the fork's own unit tests are named here and actually run. Scoped to
      # the fork's files: upstream's suite is long and is not what this gate is
      # for.
      - name: Zone CRM server tests
        run: npx nx run twenty-server:test -- --testPathPattern='imap-smtp-caldav'

  front:
    timeout-minutes: 40
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        uses: ./.github/actions/yarn-install
      - name: Build libraries
        run: |
          npx nx build twenty-shared
          npx nx build twenty-ui
          npx nx build twenty-front-component-renderer
      - name: Front typecheck
        run: npx nx run twenty-front:typecheck
      - name: Front lint
        run: npx nx run twenty-front:lint
