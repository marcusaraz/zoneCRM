// The Stitch person record fixes the left column at 380, measured from
// pm/briefs/zone-crm-desktop/person-record.html: `w-[380px] shrink-0`. The
// search bar above it takes the same width, so the column reads as one thing
// from its top edge down.
export const PAGE_LAYOUT_LEFT_PANEL_CONTAINER_WIDTH = 380;
